# Tillgängliga platshållare: {TotalDomains} {DefaultDomain}
# Redigera texten fritt. Behåll {platshållare} där du vill att siffrorna ska visas.
# Rader som börjar med # är kommentarer och ignoreras.

{TotalDomains} verifierade domäner är registrerade i miljön. Standarddomänen är {DefaultDomain}.
